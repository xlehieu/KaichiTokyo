@model IEnumerable<Category>
@if (Model != null)
{
    <ul style="display: none;">
        @foreach (var item in Model)
        {
            string url = $"/Danh-muc/{item.Alias}?catId={item.CatId}";
            <li><a href="@url">@item.CatName</a></li>
        }
    </ul>
}